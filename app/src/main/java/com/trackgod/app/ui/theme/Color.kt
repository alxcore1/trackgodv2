package com.trackgod.app.ui.theme

import androidx.compose.ui.graphics.Color

// === Background (The Void) ===
val Void = Color(0xFF131313)          // Base canvas
val VoidDeep = Color(0xFF0E0E0E)      // Recessed areas, inputs
val SurfaceLow = Color(0xFF1C1B1B)    // Cards
val SurfaceMid = Color(0xFF201F1F)    // Mid-level
val SurfaceHigh = Color(0xFF2A2A2A)   // Hover states
val SurfaceHighest = Color(0xFF353534) // Active states
val SurfaceBright = Color(0xFF3A3939) // Max elevation

// === Red (The Blood) ===
val Blood = Color(0xFF8B0000)         // Primary container, CTAs, borders
val BloodDeep = Color(0xFF690000)     // Text on primary
val BloodBright = Color(0xFFFFB4A8)   // Accent text, labels
val BloodGlow = Color(0xFFFF907F)     // Glow effects at 10% opacity
val BloodWarm = Color(0xFFE3BEB8)     // On-surface variant

// === Text (The Signal) ===
val TextPrimary = Color(0xFFE5E2E1)   // High contrast
val TextSecondary = Color(0xFFC8C6C6) // 60-70%
val TextTertiary = Color(0xFFAA8984)  // Labels, metadata
val GhostBorder = Color(0xFF5A403C)   // 15% opacity outlines

// === Semantic ===
val ErrorColor = Color(0xFFFFB4AB)
